#!/var/jb/bin/sh

for binary in /var/containers/Bundle/Application/*/pEp.app/pEp; do
    if [ -x "$binary" ]; then
        export PEP_HEADLESS_NOTIFIER=1
        exec "$binary"
    fi
done

echo "pep-native-launcher: pEp application executable was not found" >&2
exit 72
