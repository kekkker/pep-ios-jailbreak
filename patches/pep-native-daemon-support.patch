diff --git a/SubProjects/MessageModel/MessageModel/MessageModel/Services/NetworkService/Model/CoreDataPantomimeAdapter.swift b/SubProjects/MessageModel/MessageModel/MessageModel/Services/NetworkService/Model/CoreDataPantomimeAdapter.swift
index a7055e7f8..48681a972 100644
--- a/SubProjects/MessageModel/MessageModel/MessageModel/Services/NetworkService/Model/CoreDataPantomimeAdapter.swift
+++ b/SubProjects/MessageModel/MessageModel/MessageModel/Services/NetworkService/Model/CoreDataPantomimeAdapter.swift
@@ -372,14 +372,23 @@ extension CoreDataPantomimeAdapter: CWIMAPCache {
                 previousHighestUID > 0 &&
                 message.uid() > previousHighestUID &&
                 me.cdMessage(withUID: message.uid()) == nil
-            CdMessage.insertOrUpdate(pantomimeMessage: message,
-                                     account: cdAccount,
-                                     messageUpdate: messageUpdate,
-                                     context: me.privateMOC)
+            let insertedMessage = CdMessage.insertOrUpdate(
+                pantomimeMessage: message,
+                account: cdAccount,
+                messageUpdate: messageUpdate,
+                context: me.privateMOC)
             if isNewInboxMessage {
+                let sender = insertedMessage?.from?.userName ??
+                    insertedMessage?.from?.address ??
+                    "New email"
+                let subject = insertedMessage?.shortMessage ?? "(No subject)"
                 NotificationCenter.default.post(
                     name: Notification.Name("pEpNewInboxMessagePersisted"),
-                    object: nil)
+                    object: nil,
+                    userInfo: [
+                        "sender": sender,
+                        "subject": subject
+                    ])
             }
         }
     }
diff --git a/SubProjects/MessageModel/MessageModel/MessageModel/Services/Utils/BackgroundTaskManager.swift b/SubProjects/MessageModel/MessageModel/MessageModel/Services/Utils/BackgroundTaskManager.swift
index 1b653d62f..7a49fa7a9 100644
--- a/SubProjects/MessageModel/MessageModel/MessageModel/Services/Utils/BackgroundTaskManager.swift
+++ b/SubProjects/MessageModel/MessageModel/MessageModel/Services/Utils/BackgroundTaskManager.swift
@@ -69,6 +69,10 @@ extension BackgroundTaskManager {
 class BackgroundTaskManager {
     /// Bookholding: task IDs for clients.
     private var runningTasks = [AnyHashable:UIBackgroundTaskIdentifier]()
+
+    private var isRunningHeadless: Bool {
+        return ProcessInfo.processInfo.environment["PEP_HEADLESS_NOTIFIER"] == "1"
+    }
 }
 
 // MARK: - BackgroundTaskManagerProtocol
@@ -85,6 +89,13 @@ extension BackgroundTaskManager: BackgroundTaskManagerProtocol {
             throw BackgroundTaskManager.ManagingError.backgroundTaskAlreadyRunning
         }
 
+        // The jailbreak daemon is not a UIApplication and must not ask UIKit for
+        // background execution time. launchd owns its lifetime instead.
+        if isRunningHeadless {
+            runningTasks[client] = .invalid
+            return
+        }
+
         let createe = UIApplication.shared.beginBackgroundTask(withName: "\(client)") { [weak self]  in
             guard let me = self else {
                 Log.shared.errorAndCrash("Lost myself")
@@ -120,6 +131,9 @@ extension BackgroundTaskManager: BackgroundTaskManagerProtocol {
             // We are not aware of a running task for this client. Throw!
             throw BackgroundTaskManager.ManagingError.unknownClient
         }
+        if isRunningHeadless {
+            return
+        }
         UIApplication.shared.endBackgroundTask(endedTaskID)
         Log.shared.info("Did end backgroundtask with id: %@ for service: %@",
         "\(endedTaskID)", client.debugDescription)
diff --git a/pEpForiOS/AppDelegate.swift b/pEpForiOS/AppDelegate.swift
index 27fd135bb..98abb7674 100644
--- a/pEpForiOS/AppDelegate.swift
+++ b/pEpForiOS/AppDelegate.swift
@@ -9,2 +9,3 @@
 import BackgroundTasks
+import Darwin
 import UserNotifications
@@ -14,3 +15,5 @@
 import pEp4iosIntern
-
+@_silgen_name("notify_post")
+private func systemNotifyPost(_ name: UnsafePointer<CChar>) -> UInt32
+
 @UIApplicationMain
@@ -19,7 +20,10 @@ class AppDelegate: UIResponder, UIApplicationDelegate {
 
     private var newMailObserver: NSObjectProtocol?
     private var pendingNewMailCount = 0
+    private var pendingNewMailSender = "New email"
+    private var pendingNewMailSubject = "(No subject)"
     private var newMailNotificationWorkItem: DispatchWorkItem?
+    private let daemonOwnership = PEPDaemonOwnership()
 
     /// The model
     private var messageModelService: MessageModelServiceProtocol?
@@ -86,6 +90,9 @@ extension AppDelegate {
             // and pretty much don't do anything.
             return false
         }
+        guard daemonOwnership.acquireForGUI() else {
+            return false
+        }
         Log.shared.verboseLoggingEnabled = AppSettings.shared.verboseLogginEnabled
 
         Log.shared.logDebugInfo()
@@ -311,13 +318,17 @@ extension AppDelegate {
         newMailObserver = NotificationCenter.default.addObserver(
             forName: Notification.Name("pEpNewInboxMessagePersisted"),
             object: nil,
-            queue: .main) { [weak self] _ in
-                self?.queueNewMailNotification()
+            queue: .main) { [weak self] notification in
+                let sender = notification.userInfo?["sender"] as? String
+                let subject = notification.userInfo?["subject"] as? String
+                self?.queueNewMailNotification(sender: sender, subject: subject)
             }
     }
 
-    private func queueNewMailNotification() {
+    private func queueNewMailNotification(sender: String?, subject: String?) {
         pendingNewMailCount += 1
+        pendingNewMailSender = sender?.isEmpty == false ? sender! : "New email"
+        pendingNewMailSubject = subject?.isEmpty == false ? subject! : "(No subject)"
         newMailNotificationWorkItem?.cancel()
 
         let workItem = DispatchWorkItem { [weak self] in
@@ -327,7 +338,10 @@ extension AppDelegate {
             let count = me.pendingNewMailCount
             me.pendingNewMailCount = 0
             me.newMailNotificationWorkItem = nil
-            UserNotificationTool.postUserNotification(forIncomingMails: count)
+            UserNotificationTool.postUserNotification(
+                sender: me.pendingNewMailSender,
+                subject: me.pendingNewMailSubject,
+                count: count)
             Log.shared.warn("Notifications: delivered for %d newly persisted Inbox message(s)",
                             count)
         }
@@ -343,6 +357,52 @@ extension AppDelegate {
     }
 }
 
+/// Serializes access to pEp's Core Data store between the GUI process and the
+/// jailbreak daemon. The GUI asks the daemon to stop, then waits until the
+/// daemon has committed and exited before constructing MessageModel.
+private final class PEPDaemonOwnership {
+    private static let appGroupIdentifier = "group.software.pEp"
+    private static let takeoverNotification = "software.pep.native-notifier.takeover"
+    private var lockFileDescriptor: Int32 = -1
+
+    func acquireForGUI() -> Bool {
+        guard lockFileDescriptor == -1 else {
+            return true
+        }
+        guard let container = FileManager.default.containerURL(
+            forSecurityApplicationGroupIdentifier: Self.appGroupIdentifier) else {
+            NSLog("pEp daemon ownership: app-group container unavailable")
+            return false
+        }
+
+        let lockURL = container.appendingPathComponent("pep-mail-engine.lock")
+        let descriptor = open(lockURL.path, O_CREAT | O_RDWR, S_IRUSR | S_IWUSR)
+        guard descriptor >= 0 else {
+            NSLog("pEp daemon ownership: unable to open lock file (errno %d)", errno)
+            return false
+        }
+
+        Self.takeoverNotification.withCString {
+            _ = systemNotifyPost($0)
+        }
+        while flock(descriptor, LOCK_EX) != 0 {
+            if errno != EINTR {
+                NSLog("pEp daemon ownership: unable to acquire lock (errno %d)", errno)
+                close(descriptor)
+                return false
+            }
+        }
+        lockFileDescriptor = descriptor
+        return true
+    }
+
+    deinit {
+        if lockFileDescriptor >= 0 {
+            close(lockFileDescriptor)
+        }
+    }
+}
+
 // MARK: - Foreground Notifications
 
 extension AppDelegate: UNUserNotificationCenterDelegate {
diff --git a/pEpForiOS/Util/Extensions/UserNotificationTool+pEp.swift b/pEpForiOS/Util/Extensions/UserNotificationTool+pEp.swift
index d4c0d7204..349891bd0 100644
--- a/pEpForiOS/Util/Extensions/UserNotificationTool+pEp.swift
+++ b/pEpForiOS/Util/Extensions/UserNotificationTool+pEp.swift
@@ -11,6 +11,15 @@ import Foundation
 /// pEp 4 iOS specific Extensions
 extension UserNotificationTool {
 
+    static func postUserNotification(sender: String, subject: String, count: Int) {
+        guard count > 0 else {
+            return
+        }
+        let title = count == 1 ? sender : "\(sender) (+\(count - 1) more)"
+        let currentBadge = max(0, UIApplication.shared.applicationIconBadgeNumber)
+        post(title: title, body: subject, batch: currentBadge + count)
+    }
+
     static func postUserNotification(forIncomingMails numMails: Int) {
         guard numMails > 0 else {
             return
